#block-text.block
  .content
    %h2.title Home
    .inner
      %p.first
        = show_activity_count @activity_count
      %p.first
        - @activity_hash.each do |status, list|
          %br= status + " - " + list.size.to_s 
- content_for :sidebar, render(:partial => 'sidebar')
